import React from 'react';

const Sample = () => {
	return (
		<div className=" flex flex-col bg-gray-400 text-red-700 font-bold">
			Sample
			<button className="">버튼</button>
		</div>
	);
};

export default Sample;
